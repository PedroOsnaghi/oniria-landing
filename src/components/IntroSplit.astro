---

---

<section
  class="rounded-xl border border-white/12 bg-gray-700/[0.03] mt-3 backdrop-blur-md p-8 shadow-[0_8px_40px_rgba(0,0,0,.25)]"
>
  <div
    class="relative mx-auto max-w-6xl flex justify-center"
    data-component="IntroSplit"
  >
    <div class="flex flex-col">
      <div class="md:max-w-xl">
        <p
          class="font-inter text-transparent bg-clip-text bg-gradient-to-r from-violet-500 to-30% to-violet-300 text-[16px]"
        >
          Explorá tu subconsciente
        </p>
        <h2
          class="font-righteous mt-2 text-2xl md:text-[32px] text-transparent bg-clip-text bg-gradient-to-r from-gray-400 to-30% to-gray-200 font-display leading-tight tracking-tight"
        >
          ¿Alguna vez despertaste recordando un sueño y no supiste qué
          significaba?
        </h2>
        <p
          class="font-inter text-[18px] mt-3 text-neutral-300 font-display leading-tight tracking-tight"
        >
          Cada noche, tu mente te habla en formas de emociones, símbolos y
          metáforas. Oniria te ayuda a traducir ese lenguaje interior.
        </p>
      </div>

      <div
        class="flex flex-col md:flex-row gap-3 mt-28"
        data-highlighter
        aria-live="polite"
      >
        <div class="card-slide" id="card-slide">
          <div class="card">
            <!-- Gradiente interactivo -->
            <div class="card-gradient-dynamic"></div>

            <!-- Partículas animadas -->
            <div class="card-particles relative">
              <canvas
                class="absolute inset-0 z-10"
                data-particle-animation
                data-particle-quantity="3"></canvas>
            </div>

            <!-- Gradiente decorativo fijo -->
            <div class="card-gradient" aria-hidden="true">
              <div class="card-gradient-inner"></div>
            </div>

            <!-- Contenido principal -->
            <div class="card-content">
              <img
                class="card-icon"
                src="./images/carousel-icon-01.svg"
                width="56"
                height="56"
                alt="Icon 01"
              />
              <div class="card-text">
                <h3 class="card-title">Tus sueños son mensajes disfrazados.</h3>
                <p class="card-description">
                  Oniria te ayuda a descifrarlos y a entender qué emociones los
                  originan.
                </p>
              </div>
              <!-- <div class="card-action">
              <a href="#0" class="card-link">
                Learn More <span class="arrow">→</span>
              </a>
            </div> -->
            </div>
          </div>
        </div>
        <div class="card-slide" id="card-slide">
          <div class="card">
            <!-- Gradiente interactivo -->
            <div class="card-gradient-dynamic"></div>

            <!-- Partículas animadas -->
            <div class="card-particles relative">
              <canvas
                class="absolute inset-0 z-10"
                data-particle-animation
                data-particle-quantity="3"></canvas>
            </div>

            <!-- Gradiente decorativo fijo -->
            <div class="card-gradient" aria-hidden="true">
              <div class="card-gradient-inner"></div>
            </div>

            <!-- Contenido principal -->
            <div class="card-content">
              <img
                class="card-icon"
                src="./images/carousel-icon-03.svg"
                width="56"
                height="56"
                alt="Icon 01"
              />
              <div class="card-text">
                <h3 class="card-title">
                  Visualizá tu mente en movimiento<span class="inline md:hidden"
                    >.</span
                  >
                  <span class="hidden md:block">constante.</span>
                </h3>
                <p class="card-description">
                  Cada sueño se convierte en un mapa dinámico de nodos
                  interconectados.
                </p>
              </div>
              <!-- <div class="card-action">
              <a href="#0" class="card-link">
                Learn More <span class="arrow">→</span>
              </a>
            </div> -->
            </div>
          </div>
        </div>
        <div class="card-slide" id="card-slide">
          <div class="card">
            <!-- Gradiente interactivo -->
            <div class="card-gradient-dynamic"></div>

            <!-- Partículas animadas -->
            <div class="card-particles relative">
              <canvas
                class="absolute inset-0 z-10"
                data-particle-animation
                data-particle-quantity="3"></canvas>
            </div>

            <!-- Gradiente decorativo fijo -->
            <div class="card-gradient" aria-hidden="true">
              <div class="card-gradient-inner"></div>
            </div>

            <!-- Contenido principal -->
            <div class="card-content h-full">
              <img
                class="card-icon"
                src="./images/carousel-icon-04.svg"
                width="56"
                height="56"
                alt="Icon 04"
              />
              <div class="card-text hfull">
                <h3 class="card-title">Tus sueños son mensajes disfrazados.</h3>
                <p class="card-description">
                  Oniria te ayuda a descifrarlos y a entender qué emociones los
                  originan.
                </p>
              </div>
              <!-- <div class="card-action">
              <a href="#0" class="card-link">
                Learn More <span class="arrow">→</span>
              </a>
            </div> -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Tipos para la clase Highlighter
  interface MousePosition {
    x: number;
    y: number;
  }

  interface ContainerSize {
    w: number;
    h: number;
  }

  // Box highlighter
  class Highlighter {
    private container: HTMLElement;
    private boxes: Element[];
    private mouse: MousePosition;
    private containerSize: ContainerSize;

    constructor(containerElement: HTMLElement) {
      this.container = containerElement;
      this.boxes = Array.from(this.container.children);
      this.mouse = {
        x: 0,
        y: 0,
      };
      this.containerSize = {
        w: 0,
        h: 0,
      };
      this.initContainer = this.initContainer.bind(this);
      this.onMouseMove = this.onMouseMove.bind(this);
      this.init();
    }

    initContainer(): void {
      this.containerSize.w = this.container.offsetWidth;
      this.containerSize.h = this.container.offsetHeight;
    }

    onMouseMove(event: MouseEvent): void {
      const { clientX, clientY } = event;
      const rect = this.container.getBoundingClientRect();
      const { w, h } = this.containerSize;
      const x = clientX - rect.left;
      const y = clientY - rect.top;
      const inside = x < w && x > 0 && y < h && y > 0;
      if (inside) {
        this.mouse.x = x;
        this.mouse.y = y;
        this.boxes.forEach((box: Element) => {
          const boxElement = box as HTMLElement;
          const boxX =
            -(boxElement.getBoundingClientRect().left - rect.left) +
            this.mouse.x;
          const boxY =
            -(boxElement.getBoundingClientRect().top - rect.top) + this.mouse.y;
          boxElement.style.setProperty("--mouse-x", `${boxX}px`);
          boxElement.style.setProperty("--mouse-y", `${boxY}px`);
        });
      }
    }

    init(): void {
      this.initContainer();
      window.addEventListener("resize", this.initContainer);
      window.addEventListener("mousemove", this.onMouseMove);
    }
  }

  // Init Highlighter
  const highlighters = document.querySelectorAll("[data-highlighter]");
  highlighters.forEach((highlighter) => {
    if (highlighter instanceof HTMLElement) {
      new Highlighter(highlighter);
    }
  });
</script>
